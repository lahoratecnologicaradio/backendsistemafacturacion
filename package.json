{
  "name": "pos-backend",
  "version": "1.0.0",
  "description": "POS - point of sale of a Pharmacy",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "preinstall": "yarn cache clean || true",
    "postinstall": "node -e \"require('fs').accessSync(require.resolve('debug/src/common'))\" || (console.error('debug/src/common no existe. Instalación corrupta.'), process.exit(1))",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.2",
    "mysql2": "^3.14.3",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "engines": {
    "node": "20.x"
  },
  "resolutions": {
    "debug": "4.3.6"
  }
}
